# Contributing

1. Create a focused branch from `main`.
2. Keep network-engine changes separate from UI-only changes.
3. Run `npm run check` before opening a pull request.
4. Do not commit credentials, engine binaries, packet captures, or user logs.
5. Add tests for routing-rule or profile-validation changes.

Contributions are accepted under AGPL-3.0-or-later.

